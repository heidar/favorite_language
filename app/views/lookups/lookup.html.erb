<div class="lookup row">
  <div class="col-sm-4 col-sm-offset-4">
    <%= render 'form' %>

    <% if @lookup.persisted? %>
      <p class="lookup__result text-center"><%= @lookup.favorite_language %></p>
    <% end %>
  </div>
</div>
